{
  "name": "somnia-demo",
  "version": "1.0.0",
  "description": "Simple counter demo with wallet integration",
  "main": "app.js",
  "scripts": {
    "start": "npm run build && node app.js",
    "dev": "npm run build && nodemon app.js",
    "build": "node scripts/build-frontend.js",
    "build:dev": "NODE_ENV=development node scripts/build-frontend.js",
    "build:prod": "NODE_ENV=production node scripts/build-frontend.js",
    "clean": "rm -f public/index-old.html"
  },
  "dependencies": {
    "express": "^4.18.2",
    "web3": "^4.3.0",
    "cors": "^2.8.5",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
} 