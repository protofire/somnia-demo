{
  "name": "somnia-demo",
  "version": "1.0.0",
  "description": "Simple counter demo with wallet integration",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "echo 'Build completed - static files ready for deployment'",
    "build:subgraph": "cd subgraph && npm run build",
    "install:all": "npm install && cd subgraph && npm install",
    "test": "echo 'No tests configured yet'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "express": "^4.18.2",
    "web3": "^4.3.0",
    "cors": "^2.8.5",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 